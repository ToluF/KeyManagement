const express = require("express");
const router = express.Router();
const key = require("../controllers/keys");


//lookup user types
// app.get("/types", getUserTypes);
// router.get("/keys", key.getAllKeys);
// router.get("/availableKeys", key.getAvailableKeys);
// router.get("/search", key.searchKeys);
// router.get("/keyHistory/:id", key.getKeyHistory);


module.exports = router;
